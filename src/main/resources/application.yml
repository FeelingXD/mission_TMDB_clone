spring:
  application:
    name: mission

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: "jdbc:mysql://localhost:3306/TMDB?useSSL=false"
    username: "root"
    password: "12345678"

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql : true

